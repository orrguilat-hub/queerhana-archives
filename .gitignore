# Internal / non-public — never publish (Cloudflare serves the repo root)
CLAUDE.md
rights-disclaimer-draft.md
internal/
Design/

# Local review aids — human-eyes tools, never deployed
review-orientation.html

# OS cruft
.DS_Store

# Secrets / credentials — never commit
.env
.env.*
credentials
credentials.*
*.key
.netrc
ia.ini
.ia
